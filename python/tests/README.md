# Validation Tests

At this time the purpose of the validation tests is only to verify the `cholmod` implementation is correct. This is compared against `sksparse` which has similar but fewer `cholmod` functions.
